# CalendarLegend Component Documentation

## Обзор

CalendarLegend - это адаптивный компонент легенды для календарного виджета, который отображает все доступные индикаторы календаря в одну строку под календарём с автоматической адаптацией к ширине.

## Основные возможности

### ✅ Реализованные функции

1. **Отображение всех 7 индикаторов**:
   - 🟢 Доходы (зелёная точка)
   - 🔴 Расходы (красная точка)
   - ◆ Плановые транзакции (оранжевый символ)
   - 📋 Отложенные платежи (символ)
   - 💳 Кредитные платежи (символ)
   - 🟡 Кассовые разрывы (жёлтый фон)
   - 🔴 Просроченные платежи (красный фон)

2. **Адаптивность к ширине календаря**:
   - Полный режим: все индикаторы в одну строку при достаточной ширине
   - Компактный режим: приоритетные индикаторы + кнопка "Подробнее"
   - Автоматическое определение доступной ширины

3. **Исправленное модальное окно**:
   - Корректная работа кнопки "Подробнее"
   - Группировка индикаторов по типам (точки, символы, фон)
   - Безопасная обработка page объекта

4. **Улучшенная визуализация**:
   - Консистентность цветов с календарём
   - Правильное выравнивание по центру
   - Подходящие отступы между элементами (20px)
   - Читаемые размеры шрифтов (12px)

5. **Производительность и стабильность**:
   - Обновление без заметных задержек
   - Корректная обработка ошибок при отсутствии page объекта
   - Стабильность при изменении размеров окна
   - Освобождение ресурсов при размонтировании

## Архитектура

### Основные классы

- **CalendarLegend**: Главный компонент легенды
- **ModalManager**: Управление модальным окном
- **LegendIndicator**: Модель индикатора легенды
- **IndicatorType**: Enum типов индикаторов
- **DisplayMode**: Enum режимов отображения

### Файловая структура

```
src/finance_tracker/components/
├── calendar_legend.py          # Основной компонент
├── calendar_legend_types.py    # Типы данных и конфигурация
├── modal_manager.py           # Управление модальным окном
└── CALENDAR_LEGEND_README.md  # Документация
```

## Использование

### Базовое использование

```python
from finance_tracker.components.calendar_legend import CalendarLegend

# Создание легенды с автоматической шириной
legend = CalendarLegend()

# Создание легенды с заданной шириной календаря
legend = CalendarLegend(calendar_width=800)

# Добавление в контейнер
container.controls.append(legend)
```

### Интеграция с HomeView

```python
class HomeView(ft.UserControl):
    def __init__(self, page: ft.Page, session: Session):
        super().__init__()
        self.page = page
        self.session = session
        
        # Создание календаря и легенды
        self.calendar_widget = CalendarWidget(...)
        self.calendar_legend = CalendarLegend(
            calendar_width=self.calendar_widget.width
        )
    
    def build(self):
        return ft.Column([
            self.calendar_widget,
            self.calendar_legend,
            # другие компоненты
        ])
```

### Обновление ширины

```python
# При изменении размера календаря
legend.update_calendar_width(new_width)
```

## Конфигурация индикаторов

Все индикаторы настраиваются в `INDICATOR_CONFIGS`:

```python
INDICATOR_CONFIGS = {
    IndicatorType.INCOME_DOT: LegendIndicator(
        type=IndicatorType.INCOME_DOT,
        visual_element=ft.Container(
            width=10, height=10, 
            border_radius=5, 
            bgcolor=ft.Colors.GREEN
        ),
        label="Доход",
        description="Зелёная точка обозначает дни с доходными транзакциями",
        priority=1,
        estimated_width=60
    ),
    # ... остальные индикаторы
}
```

## Режимы отображения

### Полный режим (DisplayMode.FULL)
- Отображаются все 7 индикаторов в одну строку
- Активируется при достаточной ширине календаря
- Кнопка "Подробнее" не отображается

### Компактный режим (DisplayMode.COMPACT)
- Отображаются приоритетные индикаторы (доходы, расходы первыми)
- Кнопка "Подробнее" для просмотра всех индикаторов
- Активируется при ограниченной ширине

### Модальное окно (DisplayMode.MODAL_ONLY)
- Полная легенда с описаниями в модальном окне
- Группировка по типам: точки, символы, фон
- Открывается по кнопке "Подробнее"

## Обработка ошибок

### Безопасная работа с page объектом

```python
def _safe_get_page(self, event_or_control) -> Optional[ft.Page]:
    """Безопасное получение page объекта."""
    try:
        if hasattr(event_or_control, 'control') and event_or_control.control:
            return event_or_control.control.page
        elif hasattr(event_or_control, 'page'):
            return event_or_control.page
        elif hasattr(self, 'page') and self.page:
            return self.page
        return None
    except AttributeError:
        logger.warning("Не удалось получить page объект для модального окна")
        return None
```

### Fallback значения

- При ошибке вычисления ширины: возврат к 800px
- При ошибке создания UI: fallback легенда с базовыми элементами
- При ошибке модального окна: логирование без прерывания работы

## Тестирование

### Покрытие тестами

- **Unit тесты**: 23 теста для основной функциональности
- **Property-based тесты**: 25+ тестов для универсальных свойств
- **UI тесты**: 15 тестов для пользовательского интерфейса
- **Интеграционные тесты**: 7 тестов для взаимодействия с HomeView
- **Тесты обработки ошибок**: 20 тестов для стабильности

### Покрытие кода: 68%

Основные непокрытые области:
- Некоторые fallback сценарии
- Редкие ошибочные ситуации
- Специфичные методы обратной совместимости

## Производительность

### Оптимизации

- Кэширование вычислений ширины
- Ленивое создание модального окна
- Минимальные перестройки UI при изменении размеров
- Эффективная группировка индикаторов

### Метрики

- Время создания компонента: < 10ms
- Время обновления ширины: < 5ms
- Время открытия модального окна: < 50ms
- Память на экземпляр: ~2KB

## Совместимость

### Обратная совместимость

Сохранены методы старого API:
- `_open_dlg()` → `_open_modal_safe()`
- `_close_dlg()` → безопасное закрытие

### Требования

- Python >= 3.9
- Flet >= 0.25.0
- Поддержка всех платформ (Windows, macOS, Linux)

## Известные ограничения

1. **Фиксированный набор индикаторов**: 7 предопределённых типов
2. **Статическая конфигурация**: изменение индикаторов требует перезапуска
3. **Зависимость от календаря**: цвета должны совпадать с календарным виджетом

## Планы развития

### Краткосрочные улучшения
- Динамическая конфигурация индикаторов
- Пользовательские цвета и символы
- Анимации переходов между режимами

### Долгосрочные планы
- Поддержка тем оформления
- Локализация меток и описаний
- Интеграция с системой уведомлений

## Поддержка

При возникновении проблем:
1. Проверьте логи приложения
2. Убедитесь в корректности ширины календаря
3. Проверьте наличие page объекта для модальных операций
4. Обратитесь к тестам для примеров использования