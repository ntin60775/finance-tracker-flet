# Requirements Document

## Introduction

Реорганизация главного экрана приложения Finance Tracker с переходом на вертикальный календарь и новую компоновку колонок. Вертикальный календарь (дни недели по вертикали, недели по горизонтали) лучше использует доступное пространство в узкой центральной колонке. Ячейки календаря будут вертикально вытянутыми (aspect ratio ~1.3-1.5), что позволит разместить больше информации при сохранении общей площади. Новая компоновка выделяет отдельную колонку для отложенных платежей, улучшая визуальную организацию.

## Glossary

- **Vertical_Calendar**: Календарь с транспонированной сеткой, где дни недели расположены по вертикали (7 строк), а недели месяца - по горизонтали (4-6 столбцов)
- **Home_View**: Главный экран приложения с календарём и виджетами
- **Calendar_Widget**: Компонент календаря для отображения дней месяца
- **Planned_Transactions_Widget**: Виджет плановых транзакций
- **Pending_Payments_Widget**: Виджет отложенных платежей
- **Transactions_Panel**: Панель транзакций выбранного дня
- **Calendar_Legend**: Легенда календаря с объяснением индикаторов
- **Day_Cell**: Ячейка календаря, представляющая один день
- **Week_Column**: Столбец календаря, представляющий одну неделю
- **Weekday_Row**: Строка календаря, представляющая один день недели

## Requirements

### Requirement 1: Вертикальный календарь

**User Story:** Как пользователь, я хочу видеть календарь с днями недели по вертикали и неделями по горизонтали, чтобы ячейки были крупнее и читабельнее в узкой колонке.

#### Acceptance Criteria

1. WHEN Calendar_Widget отображается, THE System SHALL показывать дни недели (Пн-Вс) в виде 7 строк по вертикали
2. WHEN Calendar_Widget отображается, THE System SHALL показывать недели месяца в виде 4-6 столбцов по горизонтали
3. WHEN пользователь просматривает календарь, THE System SHALL отображать метки дней недели (Пн, Вт, Ср, Чт, Пт, Сб, Вс) слева от строк
4. WHEN пользователь просматривает календарь, THE System SHALL отображать номера недель или метки "Н1", "Н2" и т.д. над столбцами
5. WHEN месяц содержит дни из предыдущего/следующего месяца, THE System SHALL отображать пустые ячейки в соответствующих позициях
6. WHEN пользователь кликает на Day_Cell, THE System SHALL выделять выбранную дату и обновлять панель транзакций
7. WHEN Day_Cell содержит транзакции или события, THE System SHALL отображать соответствующие индикаторы (зелёные/красные точки, иконки)

### Requirement 2: Вертикально вытянутые ячейки календаря

**User Story:** Как пользователь, я хочу видеть вертикально вытянутые ячейки календаря, чтобы они лучше использовали доступное пространство и индикаторы были хорошо видны.

#### Acceptance Criteria

1. WHEN Day_Cell отображается, THE System SHALL использовать aspect_ratio примерно 1.3-1.5 для создания вертикально вытянутых ячеек
2. WHEN Day_Cell отображается, THE System SHALL сохранять общую площадь ячейки примерно равной площади квадратной ячейки (уменьшение ширины компенсируется увеличением высоты)
3. WHEN индикаторы транзакций отображаются в Day_Cell, THE System SHALL размещать их с достаточным пространством для читабельности
4. WHEN номер дня отображается в Day_Cell, THE System SHALL использовать размер шрифта не менее 14px
5. WHEN Day_Cell содержит множественные индикаторы, THE System SHALL располагать их горизонтально с spacing=2
6. WHEN Day_Cell содержит более 3 индикаторов, THE System SHALL располагать их в несколько строк для сохранения читабельности

### Requirement 3: Выделение выходных дней

**User Story:** Как пользователь, я хочу визуально различать выходные дни (субботу и воскресенье), чтобы быстро ориентироваться в календаре.

#### Acceptance Criteria

1. WHEN строка календаря представляет субботу или воскресенье, THE System SHALL применять отличающийся фон к метке дня недели
2. WHEN Day_Cell представляет субботу или воскресенье, THE System SHALL сохранять стандартную стилизацию ячейки (выделение только метки дня недели)
3. WHEN пользователь наводит курсор на выходной день, THE System SHALL отображать стандартный hover эффект

### Requirement 4: Новая компоновка главного экрана (11 частей)

**User Story:** Как пользователь, я хочу видеть главный экран с четырьмя отдельными колонками, чтобы информация была лучше организована и каждый виджет имел достаточно пространства.

#### Acceptance Criteria

1. WHEN Home_View отображается, THE System SHALL разделять экран на 4 колонки с пропорциями expand: 2, 2, 4, 3 (всего 11 частей)
2. WHEN Home_View отображается, THE System SHALL размещать Planned_Transactions_Widget в первой колонке (2/11 ширины)
3. WHEN Home_View отображается, THE System SHALL размещать Pending_Payments_Widget в второй колонке (2/11 ширины)
4. WHEN Home_View отображается, THE System SHALL размещать Calendar_Widget и Calendar_Legend в третьей колонке (4/11 ширины)
5. WHEN Home_View отображается, THE System SHALL размещать Transactions_Panel в четвёртой колонке (3/11 ширины)
6. WHEN Home_View отображается, THE System SHALL добавлять VerticalDivider между колонками для визуального разделения
7. WHEN Home_View отображается, THE System SHALL применять spacing=20 между колонками

### Requirement 5: Перемещение виджета отложенных платежей

**User Story:** Как пользователь, я хочу видеть отложенные платежи в отдельной колонке слева, чтобы они были всегда видны и не конкурировали за пространство с календарём.

#### Acceptance Criteria

1. WHEN Home_View инициализируется, THE System SHALL размещать Pending_Payments_Widget во второй колонке (между плановыми транзакциями и календарём)
2. WHEN Home_View инициализируется, THE System SHALL удалять Pending_Payments_Widget из центральной колонки под календарём
3. WHEN Pending_Payments_Widget отображается в новой позиции, THE System SHALL сохранять всю функциональность (добавление, исполнение, отмена, удаление)
4. WHEN пользователь взаимодействует с Pending_Payments_Widget, THE System SHALL обновлять календарь при изменении данных

### Requirement 6: Адаптация ширины календаря

**User Story:** Как пользователь, я хочу, чтобы календарь автоматически адаптировался к новой ширине колонки, чтобы использовать доступное пространство эффективно.

#### Acceptance Criteria

1. WHEN Calendar_Widget вычисляет свою ширину, THE System SHALL использовать пропорцию 4/11 от общей ширины страницы
2. WHEN Calendar_Widget вычисляет ширину, THE System SHALL вычитать отступы (spacing) и разделители (VerticalDivider)
3. WHEN размер окна изменяется, THE System SHALL пересчитывать ширину Calendar_Widget
4. WHEN Calendar_Legend отображается, THE System SHALL использовать ту же ширину, что и Calendar_Widget

### Requirement 7: Сохранение функциональности календаря

**User Story:** Как пользователь, я хочу, чтобы все существующие функции календаря работали после изменения ориентации, чтобы не потерять привычную функциональность.

#### Acceptance Criteria

1. WHEN пользователь кликает на Day_Cell, THE System SHALL вызывать callback on_date_selected с выбранной датой
2. WHEN пользователь переключает месяцы, THE System SHALL обновлять календарь с новыми данными
3. WHEN Day_Cell содержит транзакции, THE System SHALL отображать индикаторы (зелёные точки для доходов, красные для расходов)
4. WHEN Day_Cell содержит плановые вхождения, THE System SHALL отображать оранжевый ромб (◆)
5. WHEN Day_Cell содержит отложенные платежи, THE System SHALL отображать иконку 📋
6. WHEN Day_Cell содержит платежи по кредитам, THE System SHALL отображать иконку 💳
7. WHEN Day_Cell представляет день с кассовым разрывом, THE System SHALL применять жёлтый/оранжевый фон
8. WHEN Day_Cell представляет день с просроченным платежом по кредиту, THE System SHALL применять красный фон и красную рамку
9. WHEN Day_Cell представляет текущий день, THE System SHALL применять синюю рамку
10. WHEN Day_Cell представляет выбранный день, THE System SHALL применять зелёную рамку и primaryContainer фон

### Requirement 8: Обновление легенды календаря

**User Story:** Как пользователь, я хочу, чтобы легенда календаря корректно отображалась под вертикальным календарём, чтобы понимать значение индикаторов.

#### Acceptance Criteria

1. WHEN Calendar_Legend отображается, THE System SHALL размещать её под Calendar_Widget в той же колонке
2. WHEN Calendar_Legend вычисляет свою ширину, THE System SHALL использовать ширину Calendar_Widget
3. WHEN Calendar_Legend отображается, THE System SHALL показывать все существующие индикаторы (доходы, расходы, плановые, отложенные, кредиты, кассовые разрывы)
4. WHEN размер окна изменяется, THE System SHALL обновлять ширину Calendar_Legend

### Requirement 9: Тестирование вертикального календаря

**User Story:** Как разработчик, я хочу иметь тесты для вертикального календаря, чтобы гарантировать корректность работы новой компоновки.

#### Acceptance Criteria

1. WHEN тесты выполняются, THE System SHALL проверять корректность транспонирования сетки календаря
2. WHEN тесты выполняются, THE System SHALL проверять правильность расположения дней недели по вертикали
3. WHEN тесты выполняются, THE System SHALL проверять правильность расположения недель по горизонтали
4. WHEN тесты выполняются, THE System SHALL проверять корректность отображения пустых ячеек
5. WHEN тесты выполняются, THE System SHALL проверять сохранение функциональности выбора даты
6. WHEN тесты выполняются, THE System SHALL проверять корректность отображения индикаторов в новой компоновке
7. WHEN тесты выполняются, THE System SHALL проверять новые пропорции колонок Home_View (2:2:4:3)

### Requirement 10: Обратная совместимость данных

**User Story:** Как пользователь, я хочу, чтобы все мои существующие данные корректно отображались в новом календаре, чтобы не потерять информацию.

#### Acceptance Criteria

1. WHEN Calendar_Widget загружает транзакции, THE System SHALL отображать их в соответствующих Day_Cell независимо от ориентации календаря
2. WHEN Calendar_Widget загружает плановые вхождения, THE System SHALL отображать их индикаторы в правильных ячейках
3. WHEN Calendar_Widget загружает отложенные платежи, THE System SHALL отображать их индикаторы в правильных ячейках
4. WHEN Calendar_Widget загружает платежи по кредитам, THE System SHALL отображать их индикаторы в правильных ячейках
5. WHEN Calendar_Widget загружает кассовые разрывы, THE System SHALL применять правильную стилизацию к соответствующим ячейкам
