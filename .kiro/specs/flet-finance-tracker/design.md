# Design Document: Finance Tracker Flet

## Overview

Портирование веб-приложения Finance Tracker на десктопную платформу с использованием Python + Flet. Приложение представляет собой кроссплатформенный инструмент для управления личными финансами с полным функционалом оригинальной веб-версии, но с нативным UI и улучшенным UX для десктопа.

**Ключевые особенности дизайна:**
- Инкрементальная разработка по подсистемам (каждая подсистема запускаема независимо)
- Переиспользование существующих моделей данных и сервисов из веб-версии
- Чистая архитектура с разделением UI, бизнес-логики и данных
- Модульная структура для простого добавления новых подсистем

**Порядок реализации подсистем:**
1. Календарь и транзакции (базовая функциональность)
2. Категории
3. Плановые транзакции
4. Отложенные платежи
5. Займодатели и кредиты
6. План-факт анализ
7. Статистика и отчёты

## Architecture

### Общая архитектура

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                           Flet Application                                    │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │                    UI Layer (Flet Components)                          │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌───────────┐  │  │
│  │  │  Calendar    │  │  Categories  │  │   Planned    │  │  Pending  │  │  │
│  │  │    View      │  │     View     │  │Transactions  │  │ Payments  │  │  │
│  │  │              │  │              │  │     View     │  │   View    │  │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  └───────────┘  │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌───────────┐  │  │
│  │  │    Loans     │  │ Loan Details │  │  Plan-Fact   │  │  Lenders  │  │  │
│  │  │    View      │  │     View     │  │  Analysis    │  │   View    │  │  │
│  │  │              │  │              │  │     View     │  │           │  │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  └───────────┘  │  │
│  │                                                                          │  │
│  │  Переиспользуемые компоненты:                                          │  │
│  │  • CalendarWidget (календарь с индикаторами)                           │  │
│  │  • TransactionsPanel (панель транзакций выбранного дня)                │  │
│  │  • CalendarLegend (легенда календаря)                                  │  │
│  │  • PendingPaymentsWidget (виджет топ-5 отложенных платежей)            │  │
│  │  • PlannedTransactionsWidget (виджет ближайших плановых транзакций)    │  │
│  │  • TransactionModal (создание/редактирование транзакций)               │  │
│  │  • ExecuteOccurrenceModal (исполнение плановых вхождений)              │  │
│  │  • PendingPaymentModal (создание/редактирование отложенных платежей)   │  │
│  │  • ExecutePendingPaymentModal (исполнение отложенных платежей)         │  │
│  │  • PlannedTransactionModal (создание/редактирование плановых)          │  │
│  │  • CategorySelector (выбор категории)                                  │  │
│  │  • DatePicker (выбор даты)                                             │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│                                      ↕                                        │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │                 Business Logic Layer (Services)                        │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌───────────┐  │  │
│  │  │ Transaction  │  │   Category   │  │   Planned    │  │  Pending  │  │  │
│  │  │   Service    │  │   Service    │  │ Transaction  │  │  Payment  │  │  │
│  │  │              │  │              │  │   Service    │  │  Service  │  │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  └───────────┘  │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌───────────┐  │  │
│  │  │    Loan      │  │   Lender     │  │ Loan Payment │  │ Recurrence│  │  │
│  │  │   Service    │  │   Service    │  │   Service    │  │  Service  │  │  │
│  │  │              │  │              │  │              │  │           │  │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  └───────────┘  │  │
│  │  ┌──────────────┐  ┌──────────────┐                                    │  │
│  │  │   Balance    │  │  Plan-Fact   │                                    │  │
│  │  │  Forecast    │  │   Service    │                                    │  │
│  │  │   Service    │  │              │                                    │  │
│  │  └──────────────┘  └──────────────┘                                    │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│                                      ↕                                        │
│  ┌────────────────────────────────────────────────────────────────────────┐  │
│  │                  Data Layer (SQLAlchemy Models)                        │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌───────────┐  │  │
│  │  │TransactionDB │  │  CategoryDB  │  │  Planned     │  │  Pending  │  │  │
│  │  │              │  │              │  │TransactionDB │  │ PaymentDB │  │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  └───────────┘  │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌───────────┐  │  │
│  │  │   LoanDB     │  │   LenderDB   │  │ LoanPaymentDB│  │Recurrence │  │  │
│  │  │              │  │              │  │              │  │  RuleDB   │  │  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘  └───────────┘  │  │
│  │  ┌──────────────┐                                                       │  │
│  │  │   Planned    │                                                       │  │
│  │  │ OccurrenceDB │                                                       │  │
│  │  └──────────────┘                                                       │  │
│  └────────────────────────────────────────────────────────────────────────┘  │
│                                      ↕                                        │
│                              SQLite Database                                  │
└──────────────────────────────────────────────────────────────────────────────┘
```

**Принципы архитектуры:**
- **Разделение ответственности**: UI компоненты не содержат бизнес-логику
- **Переиспользование**: Сервисы и модели копируются из веб-версии без изменений
- **Модульность**: Каждая подсистема независима и может быть запущена отдельно
- **Инкрементальность**: Разработка идёт по подсистемам, каждая полностью завершается перед следующей

### Структура проекта

```
finance_tracker_flet/
├── main.py                      # Точка входа приложения
├── app.py                       # Главное приложение Flet
├── config.py                    # Конфигурация приложения
├── database.py                  # Инициализация БД
│
├── models/                      # Модели данных (копии из веб-версии)
│   ├── __init__.py
│   ├── base.py                  # Base класс SQLAlchemy
│   ├── transaction.py           # TransactionDB, Transaction, TransactionCreate
│   ├── category.py              # CategoryDB, Category, CategoryCreate
│   ├── planned_transaction.py   # PlannedTransactionDB, RecurrenceRuleDB
│   ├── pending_payment.py       # PendingPaymentDB
│   ├── loan.py                  # LoanDB, LenderDB, LoanPaymentDB
│   └── enums.py                 # Все Enum типы
│
├── services/                    # Бизнес-логика (копии из веб-версии)
│   ├── __init__.py
│   ├── transaction_service.py
│   ├── category_service.py
│   ├── planned_transaction_service.py
│   ├── recurrence_service.py
│   ├── pending_payment_service.py
│   ├── loan_service.py
│   ├── lender_service.py
│   ├── loan_payment_service.py
│   ├── balance_forecast_service.py
│   └── plan_fact_service.py
│
├── views/                       # UI компоненты (Flet)
│   ├── __init__.py
│   ├── main_window.py           # Главное окно с навигацией
│   ├── home_view.py             # Главный экран (календарь + панели)
│   ├── categories_view.py       # Управление категориями
│   ├── planned_transactions_view.py
│   ├── pending_payments_view.py
│   ├── loans_view.py
│   ├── loan_details_view.py
│   ├── lenders_view.py
│   └── plan_fact_view.py
│
├── components/                  # Переиспользуемые UI компоненты
│   ├── __init__.py
│   ├── calendar_widget.py       # Виджет календаря с индикаторами
│   ├── transactions_panel.py    # Панель транзакций выбранного дня
│   ├── calendar_legend.py       # Легенда календаря (краткая + модальная)
│   ├── pending_payments_widget.py  # Виджет топ-5 отложенных платежей
│   ├── planned_transactions_widget.py  # Виджет ближайших плановых
│   ├── transaction_modal.py     # Модальное окно транзакции
│   ├── execute_occurrence_modal.py  # Модальное окно исполнения вхождения
│   ├── pending_payment_modal.py  # Модальное окно отложенного платежа
│   ├── execute_pending_payment_modal.py  # Модальное окно исполнения платежа
│   ├── planned_transaction_modal.py  # Модальное окно плановой транзакции
│   ├── category_selector.py     # Селектор категории
│   └── date_picker.py           # Выбор даты
│
└── utils/                       # Утилиты
    ├── __init__.py
    ├── logger.py                # Настройка логирования
    ├── validators.py            # Дополнительные валидаторы
    └── formatters.py            # Форматирование дат, сумм
```

## Components and Interfaces

### 1. Главное окно и навигация (MainWindow)

**Ответственность:**
- Отображение главного окна приложения
- Управление навигацией между разделами
- Отображение текущего баланса в заголовке
- Сохранение состояния (последний раздел, размер окна)

**Интерфейс:**
```python
class MainWindow:
    def __init__(self, page: ft.Page, db_session):
        """
        Инициализация главного окна.
        
        Args:
            page: Flet страница
            db_session: Сессия базы данных
        """
        
    def build(self) -> ft.Control:
        """Построение UI главного окна."""
        
    def navigate_to(self, route: str):
        """
        Навигация к указанному разделу.
        
        Args:
            route: Маршрут раздела (calendar, categories, loans и т.д.)
        """
        
    def update_balance(self):
        """Обновление отображения текущего баланса."""
        
    def save_state(self):
        """Сохранение состояния приложения (размер окна, последний раздел)."""
```

**Компоненты:**
- Боковая панель навигации (NavigationRail или Drawer)
- Область контента (Container для текущего view)
- Заголовок с балансом (AppBar)

### 2. Главный экран (HomeView)

**Ответственность:**
- Отображение главного экрана приложения (открывается по умолчанию)
- Интеграция календаря, панелей транзакций, отложенных и плановых платежей
- Координация взаимодействия между компонентами

**Структура главного экрана:**
```
┌─────────────────────────────────────────────────────────────┐
│                      Главный экран                           │
│  ┌────────────────────────┐  ┌──────────────────────────┐   │
│  │                        │  │  Панель транзакций       │   │
│  │      Календарь         │  │  выбранного дня          │   │
│  │                        │  │                          │   │
│  │                        │  │  • Фактические           │   │
│  │                        │  │  • Плановые вхождения    │   │
│  │                        │  │  • Отложенные платежи    │   │
│  │                        │  │  • Платежи по кредитам   │   │
│  └────────────────────────┘  │                          │   │
│  ┌────────────────────────┐  │  Итоги дня               │   │
│  │  Легенда (краткая)     │  │  Прогноз баланса         │   │
│  │  [Показать полную]     │  └──────────────────────────┘   │
│  └────────────────────────┘                                  │
│  ┌────────────────────────┐  ┌──────────────────────────┐   │
│  │  Виджет отложенных     │  │  Виджет плановых         │   │
│  │  платежей (топ-5)      │  │  транзакций (ближайшие)  │   │
│  └────────────────────────┘  └──────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

**Интерфейс:**
```python
class HomeView:
    def __init__(self, db_session, services: dict):
        """
        Инициализация главного экрана.
        
        Args:
            db_session: Сессия базы данных
            services: Словарь с сервисами (transaction, planned_transaction, 
                     pending_payment, balance_forecast)
        """
        
    def build(self) -> ft.Control:
        """Построение UI главного экрана."""
        
    def on_date_selected(self, date: date):
        """
        Обработка выбора даты в календаре.
        Обновляет панель транзакций для выбранной даты.
        
        Args:
            date: Выбранная дата
        """
        
    def refresh_all_panels(self):
        """Обновление всех панелей главного экрана."""
        
    def show_legend_modal(self):
        """Отображение модального окна с полной легендой календаря."""
```

**Компоненты главного экрана:**

**2.1. CalendarWidget** - Виджет календаря
- Отображение календаря текущего месяца
- Визуальные индикаторы финансовой активности на днях
- Навигация между месяцами
- Обработка клика по дню

**Визуальные индикаторы дней:**
- 🟢 Зелёный кружок: есть доходы
- 🔴 Красный кружок: есть расходы
- 🟡 Жёлтый/оранжевый фон: кассовый разрыв (отрицательный прогноз баланса)
- 🔵 Синяя рамка: текущий день
- ⬜ Серый фон: выбранный день
- 💳 Индикатор: есть платежи по кредитам
- 📋 Индикатор: есть отложенные платежи с плановой датой
- ◆ Индикатор: есть плановые вхождения

**2.2. TransactionsPanel** - Панель транзакций выбранного дня
- Список фактических транзакций
- Список плановых вхождений (с возможностью исполнения/пропуска)
- Список отложенных платежей с плановой датой на этот день
- Список платежей по кредитам на этот день
- Итоги дня (доходы, расходы, баланс)
- Прогноз баланса на выбранную дату
- Кнопка добавления новой транзакции

**2.3. CalendarLegend** - Легенда календаря
- Краткая версия (по умолчанию): основные индикаторы
- Полная версия (модальное окно): все индикаторы с подробным описанием
- Кнопка "Показать полную легенду"

**2.4. PendingPaymentsWidget** - Виджет отложенных платежей
- Топ-5 активных платежей по приоритету
- Статистика (общая сумма, количество)
- Кнопка "Показать все" → переход в PendingPaymentsView
- Быстрые действия (исполнить, отменить)

**2.5. PlannedTransactionsWidget** - Виджет плановых транзакций
- Ближайшие 5 плановых вхождений
- Статус каждого вхождения
- Кнопка "Показать все" → переход в PlannedTransactionsView
- Быстрое исполнение вхождения

### 3. Управление категориями (CategoriesView)

**Ответственность:**
- Отображение списка категорий
- Фильтрация по типу (все, доходы, расходы)
- CRUD операции с категориями
- Защита системных категорий от удаления

**Интерфейс:**
```python
class CategoriesView:
    def __init__(self, db_session, category_service):
        """
        Инициализация представления категорий.
        
        Args:
            db_session: Сессия базы данных
            category_service: Сервис для работы с категориями
        """
        
    def build(self) -> ft.Control:
        """Построение UI списка категорий."""
        
    def filter_categories(self, filter_type: Optional[TransactionType]):
        """
        Фильтрация категорий по типу.
        
        Args:
            filter_type: Тип для фильтрации (None = все)
        """
        
    def create_category(self, name: str, type: TransactionType):
        """
        Создание новой категории.
        
        Args:
            name: Название категории
            type: Тип категории (INCOME/EXPENSE)
        """
        
    def delete_category(self, category_id: int):
        """
        Удаление категории с проверками.
        
        Args:
            category_id: ID категории для удаления
        """
```

### 4. Плановые транзакции (PlannedTransactionsView)

**Ответственность:**
- Отображение списка плановых транзакций
- Создание/редактирование плановых транзакций с правилами повторения
- Управление вхождениями (исполнение, пропуск)
- Интеграция с календарём

**Интерфейс:**
```python
class PlannedTransactionsView:
    def __init__(self, db_session, planned_transaction_service, recurrence_service):
        """
        Инициализация представления плановых транзакций.
        
        Args:
            db_session: Сессия базы данных
            planned_transaction_service: Сервис плановых транзакций
            recurrence_service: Сервис генерации вхождений
        """
        
    def build(self) -> ft.Control:
        """Построение UI списка плановых транзакций."""
        
    def create_planned_transaction(self, data: PlannedTransactionCreate):
        """
        Создание плановой транзакции.
        
        Args:
            data: Данные плановой транзакции
        """
        
    def execute_occurrence(self, occurrence_id: int, executed_date: date, executed_amount: float):
        """
        Исполнение вхождения плановой транзакции.
        
        Args:
            occurrence_id: ID вхождения
            executed_date: Фактическая дата исполнения
            executed_amount: Фактическая сумма
        """
        
    def skip_occurrence(self, occurrence_id: int, reason: str):
        """
        Пропуск вхождения.
        
        Args:
            occurrence_id: ID вхождения
            reason: Причина пропуска
        """
```

### 5. Отложенные платежи (PendingPaymentsView)

**Ответственность:**
- Отображение списка активных отложенных платежей
- Сортировка по приоритету и дате
- Исполнение и отмена платежей
- Отображение статистики

**Интерфейс:**
```python
class PendingPaymentsView:
    def __init__(self, db_session, pending_payment_service):
        """
        Инициализация представления отложенных платежей.
        
        Args:
            db_session: Сессия базы данных
            pending_payment_service: Сервис отложенных платежей
        """
        
    def build(self) -> ft.Control:
        """Построение UI списка отложенных платежей."""
        
    def create_pending_payment(self, data: PendingPaymentCreate):
        """
        Создание отложенного платежа.
        
        Args:
            data: Данные платежа
        """
        
    def execute_payment(self, payment_id: int, executed_date: date, executed_amount: Optional[float]):
        """
        Исполнение отложенного платежа.
        
        Args:
            payment_id: ID платежа
            executed_date: Дата исполнения
            executed_amount: Фактическая сумма (None = плановая)
        """
        
    def cancel_payment(self, payment_id: int, reason: Optional[str]):
        """
        Отмена платежа.
        
        Args:
            payment_id: ID платежа
            reason: Причина отмены
        """
```

### 6. Займодатели и кредиты (LoansView, LoanDetailsView)

**Ответственность:**
- Отображение списка кредитов
- Управление займодателями
- Детальная информация о кредите (график платежей, статистика)
- Управление платежами по кредитам

**Интерфейс:**
```python
class LoansView:
    def __init__(self, db_session, loan_service, lender_service):
        """
        Инициализация представления кредитов.
        
        Args:
            db_session: Сессия базы данных
            loan_service: Сервис кредитов
            lender_service: Сервис займодателей
        """
        
    def build(self) -> ft.Control:
        """Построение UI списка кредитов."""
        
    def create_loan(self, data: LoanCreate):
        """
        Создание кредита.
        
        Args:
            data: Данные кредита
        """
        
    def open_loan_details(self, loan_id: int):
        """
        Открытие детального представления кредита.
        
        Args:
            loan_id: ID кредита
        """

class LoanDetailsView:
    def __init__(self, db_session, loan_id: int, loan_payment_service):
        """
        Инициализация детального представления кредита.
        
        Args:
            db_session: Сессия базы данных
            loan_id: ID кредита
            loan_payment_service: Сервис платежей по кредитам
        """
        
    def build(self) -> ft.Control:
        """Построение UI деталей кредита."""
        
    def show_payment_schedule(self):
        """Отображение графика платежей."""
        
    def execute_payment(self, payment_id: int, executed_date: date, executed_amount: float):
        """
        Исполнение платежа по кредиту.
        
        Args:
            payment_id: ID платежа
            executed_date: Дата исполнения
            executed_amount: Сумма исполнения
        """
```

### 7. План-факт анализ (PlanFactView)

**Ответственность:**
- Отображение списка вхождений с фильтрацией
- Расчёт отклонений по сумме и дате
- Статистика исполнения бюджета

**Интерфейс:**
```python
class PlanFactView:
    def __init__(self, db_session, plan_fact_service):
        """
        Инициализация представления план-факт анализа.
        
        Args:
            db_session: Сессия базы данных
            plan_fact_service: Сервис план-факт анализа
        """
        
    def build(self) -> ft.Control:
        """Построение UI план-факт анализа."""
        
    def filter_occurrences(self, start_date: date, end_date: date, category_id: Optional[int]):
        """
        Фильтрация вхождений.
        
        Args:
            start_date: Дата начала периода
            end_date: Дата окончания периода
            category_id: ID категории (None = все)
        """
        
    def calculate_statistics(self) -> dict:
        """
        Расчёт статистики исполнения.
        
        Returns:
            Словарь со статистикой (процент вовремя, средние отклонения и т.д.)
        """
```

## Data Models

Все модели данных копируются из веб-версии (`finance_tracker_python/src/models.py`) без изменений. Используется та же схема БД SQLite.

### Основные модели

**CategoryDB** - Категории для классификации транзакций
- `id`: int (PK)
- `name`: str (unique)
- `type`: TransactionType (INCOME/EXPENSE)
- `is_system`: bool (защита от удаления)

**TransactionDB** - Фактические транзакции
- `id`: int (PK)
- `amount`: float
- `type`: TransactionType
- `category_id`: int (FK)
- `description`: str (optional)
- `transaction_date`: date
- `planned_occurrence_id`: int (FK, optional)

**PlannedTransactionDB** - Шаблоны плановых транзакций
- `id`: int (PK)
- `amount`: float
- `category_id`: int (FK)
- `description`: str (optional)
- `type`: TransactionType
- `start_date`: date
- `end_date`: date (optional)
- `is_active`: bool

**RecurrenceRuleDB** - Правила повторения
- `id`: int (PK)
- `planned_transaction_id`: int (FK, unique)
- `recurrence_type`: RecurrenceType
- `interval`: int (optional)
- `interval_unit`: IntervalUnit (optional)
- `weekdays`: str (JSON, optional)
- `only_workdays`: bool
- `end_condition_type`: EndConditionType
- `end_date`: date (optional)
- `occurrences_count`: int (optional)

**PlannedOccurrenceDB** - Вхождения плановых транзакций
- `id`: int (PK)
- `planned_transaction_id`: int (FK)
- `occurrence_date`: date
- `amount`: float
- `status`: OccurrenceStatus (PENDING/EXECUTED/SKIPPED)
- `actual_transaction_id`: int (FK, optional)
- `executed_date`: date (optional)
- `executed_amount`: float (optional)
- `skipped_date`: date (optional)
- `skip_reason`: str (optional)

**PendingPaymentDB** - Отложенные платежи
- `id`: int (PK)
- `amount`: float
- `category_id`: int (FK, только EXPENSE)
- `description`: str
- `priority`: PendingPaymentPriority
- `planned_date`: date (optional)
- `status`: PendingPaymentStatus
- `actual_transaction_id`: int (FK, optional)
- `executed_date`: date (optional)
- `executed_amount`: float (optional)
- `cancelled_date`: date (optional)
- `cancel_reason`: str (optional)

**LenderDB** - Займодатели
- `id`: int (PK)
- `name`: str (unique)
- `lender_type`: LenderType
- `description`: str (optional)
- `contact_info`: str (optional)
- `notes`: str (optional)

**LoanDB** - Кредиты
- `id`: int (PK)
- `lender_id`: int (FK)
- `name`: str
- `loan_type`: LoanType
- `amount`: float
- `interest_rate`: float (optional)
- `term_months`: int (optional)
- `issue_date`: date
- `end_date`: date (optional)
- `contract_number`: str (optional)
- `description`: str (optional)
- `status`: LoanStatus

**LoanPaymentDB** - Платежи по кредитам
- `id`: int (PK)
- `loan_id`: int (FK)
- `scheduled_date`: date
- `principal_amount`: float
- `interest_amount`: float
- `total_amount`: float
- `status`: PaymentStatus
- `actual_transaction_id`: int (FK, optional)
- `executed_date`: date (optional)
- `executed_amount`: float (optional)
- `overdue_days`: int (optional)

## Correctness Properties

*A property is a characteristic or behavior that should hold true across all valid executions of a system-essentially, a formal statement about what the system should do. Properties serve as the bridge between human-readable specifications and machine-verifiable correctness guarantees.*

### Навигация и UI

Property 1: Навигация отображает выбранный раздел
*Для любого* раздела в навигации, при выборе этого раздела система должна отображать соответствующий экран без перезагрузки приложения
**Validates: Requirements 2.3**

Property 2: Персистентность выбранного раздела
*Для любого* раздела, если пользователь выбирает раздел и перезапускает приложение, то при следующем запуске должен открыться тот же раздел
**Validates: Requirements 2.4, 13.4**

Property 3: Отображение текущего баланса
*Для любого* состояния транзакций, текущий баланс в заголовке должен равняться сумме всех доходов минус сумма всех расходов
**Validates: Requirements 2.5**

### Календарь и транзакции

Property 4: Индикаторы дней календаря
*Для любого* дня с транзакциями, если день содержит только доходы, то должен отображаться зелёный индикатор; если только расходы — красный индикатор; если оба типа — оба индикатора
**Validates: Requirements 3.2**

Property 5: Отображение транзакций дня
*Для любого* дня и любого набора транзакций, при клике на день должны отображаться только транзакции с датой равной выбранному дню
**Validates: Requirements 3.3**

Property 6: Сохранение транзакции
*Для любой* валидной транзакции, после создания транзакция должна быть сохранена в БД и доступна для чтения
**Validates: Requirements 3.4**

Property 7: Обновление транзакции
*Для любой* существующей транзакции и любых валидных изменений, после редактирования изменения должны быть сохранены в БД
**Validates: Requirements 3.5**

Property 8: Удаление транзакции
*Для любой* существующей транзакции, после удаления транзакция не должна присутствовать в БД
**Validates: Requirements 3.6**

Property 9: Навигация между месяцами
*Для любого* месяца, переход на следующий месяц должен отображать календарь следующего месяца, переход на предыдущий — предыдущего
**Validates: Requirements 3.7**

### Категории

Property 10: Фильтрация категорий по типу
*Для любого* набора категорий и любого фильтра (все/доходы/расходы), результат фильтрации должен содержать только категории соответствующего типа
**Validates: Requirements 4.2**

Property 11: Уникальность названия категории
*Для любой* новой категории, если категория с таким названием уже существует, то создание должно быть отклонено с ошибкой валидации
**Validates: Requirements 4.3**

Property 12: Защита системных категорий
*Для любой* системной категории (is_system=True), попытка удаления должна быть заблокирована с сообщением об ошибке
**Validates: Requirements 4.4**

Property 13: Защита категорий со связанными транзакциями
*Для любой* пользовательской категории, если существуют транзакции с этой категорией, то удаление категории должно быть заблокировано
**Validates: Requirements 4.5**

Property 14: Цветовая индикация типа категории
*Для любой* категории, отображение должно содержать цветовую индикацию соответствующую типу (зелёный для доходов, красный для расходов)
**Validates: Requirements 4.6**

### Плановые транзакции

Property 15: Поддержка типов повторения
*Для любого* типа повторения (однократная, ежедневная, еженедельная, ежемесячная, ежегодная, кастомная), система должна корректно создавать плановую транзакцию с этим типом
**Validates: Requirements 5.1**

Property 16: Поддержка условий окончания
*Для любого* условия окончания (бессрочно, до даты, после N повторений), система должна корректно применять это условие при генерации вхождений
**Validates: Requirements 5.2**

Property 17: Генерация вхождений
*Для любой* периодической транзакции с правилом повторения, система должна автоматически генерировать вхождения согласно правилу
**Validates: Requirements 5.3**

Property 18: Исполнение вхождения
*Для любого* вхождения со статусом PENDING, после исполнения должна быть создана фактическая транзакция, и статус вхождения должен измениться на EXECUTED
**Validates: Requirements 5.4**

Property 19: Пропуск вхождения
*Для любого* вхождения со статусом PENDING, после пропуска статус должен измениться на SKIPPED, и причина пропуска должна быть сохранена
**Validates: Requirements 5.5**

Property 20: Отображение вхождений в календаре
*Для любого* дня с плановыми вхождениями, вхождения должны отображаться в календаре с визуальными индикаторами
**Validates: Requirements 5.7**

### Прогноз баланса

Property 21: Расчёт прогнозируемого баланса
*Для любого* дня, прогнозируемый баланс должен равняться текущему балансу плюс сумма всех плановых доходов минус сумма всех плановых расходов до этого дня
**Validates: Requirements 6.1**

Property 22: Учёт компонентов в прогнозе
*Для любого* дня, прогноз баланса должен учитывать фактические транзакции, плановые вхождения и отложенные платежи с плановой датой
**Validates: Requirements 6.2**

Property 23: Визуализация кассового разрыва
*Для любого* дня с отрицательным прогнозируемым балансом, день должен быть выделен жёлтым/оранжевым цветом в календаре
**Validates: Requirements 6.3**

Property 24: Отображение баланса и прогноза
*Для любой* выбранной даты, система должна отображать текущий баланс и прогноз баланса на эту дату
**Validates: Requirements 6.4**

### План-факт анализ

Property 25: Фильтрация по периоду
*Для любого* периода (дата начала, дата окончания), результат должен содержать только вхождения с датой в указанном диапазоне
**Validates: Requirements 7.2**

Property 26: Фильтрация по категории
*Для любой* категории, результат должен содержать только вхождения плановых транзакций с указанной категорией
**Validates: Requirements 7.3**

Property 27: Расчёт отклонений
*Для любого* исполненного вхождения, отклонение по сумме должно равняться (executed_amount - planned_amount), отклонение по дате — (executed_date - scheduled_date) в днях
**Validates: Requirements 7.4**

Property 28: Расчёт статистики исполнения
*Для любого* набора вхождений, процент вовремя должен равняться (количество исполненных с отклонением по дате ≤ 1 день) / (общее количество исполненных) * 100
**Validates: Requirements 7.5**

Property 29: Отображение деталей вхождения
*Для любого* вхождения, при клике должна отображаться детальная информация включая плановые и фактические данные, отклонения
**Validates: Requirements 7.6**

### Отложенные платежи

Property 30: Валидация категории отложенного платежа
*Для любого* отложенного платежа, категория должна иметь тип EXPENSE, иначе создание должно быть отклонено с ошибкой валидации
**Validates: Requirements 8.1**

Property 31: Исполнение отложенного платежа
*Для любого* активного отложенного платежа, после исполнения должна быть создана фактическая транзакция расхода, и статус должен измениться на EXECUTED
**Validates: Requirements 8.4**

Property 32: Отмена отложенного платежа
*Для любого* активного отложенного платежа, после отмены статус должен измениться на CANCELLED, и причина отмены должна быть сохранена
**Validates: Requirements 8.5**

Property 33: Сортировка отложенных платежей
*Для любого* списка активных платежей, платежи должны быть отсортированы сначала по приоритету (Critical > High > Medium > Low), затем по плановой дате
**Validates: Requirements 8.6**

Property 34: Статистика отложенных платежей
*Для любого* набора активных платежей, общая сумма должна равняться сумме всех amount, распределение по приоритетам должно показывать количество платежей каждого приоритета
**Validates: Requirements 8.7**

### Займодатели

Property 35: Уникальность названия займодателя
*Для любого* нового займодателя, если займодатель с таким названием уже существует, то создание должно быть отклонено с ошибкой валидации
**Validates: Requirements 9.1**

Property 36: Сохранение данных займодателя
*Для любого* займодателя, контактная информация и примечания должны сохраняться в БД и быть доступны для чтения
**Validates: Requirements 9.3**

Property 37: Защита займодателей со связанными кредитами
*Для любого* займодателя, если существуют кредиты от этого займодателя, то удаление должно быть заблокировано
**Validates: Requirements 9.4**

### Кредиты

Property 38: Валидация обязательных полей кредита
*Для любого* нового кредита, если отсутствует займодатель, сумма или дата выдачи, то создание должно быть отклонено с ошибкой валидации
**Validates: Requirements 10.1**

Property 39: Расчёт даты окончания кредита
*Для любого* кредита с указанным сроком в месяцах, дата окончания должна равняться (дата выдачи + срок в месяцах)
**Validates: Requirements 10.3**

Property 40: Генерация графика платежей
*Для любого* нового кредита, система должна автоматически генерировать график платежей на основе суммы, ставки и срока
**Validates: Requirements 10.5**

### Платежи по кредитам

Property 41: Создание транзакции при исполнении платежа
*Для любого* платежа по кредиту, при исполнении должна быть создана фактическая транзакция расхода с суммой равной executed_amount
**Validates: Requirements 11.1**

Property 42: Автоматическое обновление статуса просроченных платежей
*Для любого* платежа со статусом PENDING, если текущая дата больше плановой даты платежа, то статус должен автоматически обновиться на OVERDUE
**Validates: Requirements 11.3**

Property 43: Расчёт дней просрочки
*Для любого* просроченного платежа, количество дней просрочки должно равняться (текущая дата - плановая дата) в днях
**Validates: Requirements 11.4**

Property 44: Отображение платежей в календаре
*Для любого* дня с платежами по кредитам, платежи должны отображаться в календаре с индикатором 💳
**Validates: Requirements 11.6**

Property 45: Выделение просроченных платежей
*Для любого* просроченного платежа в календаре, платёж должен быть выделен специальным образом (красный цвет/рамка)
**Validates: Requirements 11.7**

### Статистика по кредитам

Property 46: Расчёт общей суммы активных кредитов
*Для любого* набора кредитов, общая сумма активных кредитов должна равняться сумме amount всех кредитов со статусом ACTIVE
**Validates: Requirements 12.1**

Property 47: Расчёт среднемесячных платежей
*Для любого* набора активных кредитов, среднемесячные платежи должны равняться сумме всех ожидаемых платежей текущего месяца
**Validates: Requirements 12.2**

Property 48: Расчёт кредитной нагрузки
*Для любого* набора кредитов и суммы дохода, кредитная нагрузка должна равняться (среднемесячные платежи / среднемесячный доход) * 100%
**Validates: Requirements 12.3**

Property 49: Расчёт просроченной задолженности
*Для любого* набора платежей, просроченная задолженность должна равняться сумме total_amount всех платежей со статусом OVERDUE
**Validates: Requirements 12.4**

Property 50: Фильтрация статистики по периоду
*Для любого* периода, статистика должна учитывать только кредиты и платежи в указанном диапазоне дат
**Validates: Requirements 12.5**

Property 51: Распределение кредитов по типам
*Для любого* набора кредитов, распределение должно показывать количество и сумму кредитов для каждого типа (Микрокредит, Потребительский, Ипотека и т.д.)
**Validates: Requirements 12.6**

### Настройки и конфигурация

Property 52: Персистентность пути к БД
*Для любого* пути к базе данных, после сохранения в конфигурационный файл путь должен быть доступен при следующем запуске приложения
**Validates: Requirements 13.1**

Property 53: Персистентность размера и положения окна
*Для любого* размера и положения окна, после закрытия и повторного открытия приложения окно должно иметь тот же размер и положение
**Validates: Requirements 13.3**

### Обработка ошибок и валидация

Property 54: Отображение ошибок валидации
*Для любой* ошибки валидации, система должна отображать сообщение с описанием проблемы пользователю
**Validates: Requirements 14.1**

Property 55: Логирование ошибок БД
*Для любой* ошибки базы данных, система должна логировать детали ошибки и показывать общее сообщение пользователю
**Validates: Requirements 14.2**

Property 56: Валидация пользовательских вводов
*Для любого* пользовательского ввода, данные должны быть провалидированы перед сохранением в БД
**Validates: Requirements 14.3**

Property 57: Предотвращение некорректных операций
*Для любой* попытки удаления системной категории или категории со связанными транзакциями, операция должна быть заблокирована
**Validates: Requirements 14.4**

### Производительность

Property 58: Время инициализации приложения
*Для любого* запуска приложения, инициализация должна завершаться менее чем за 2 секунды
**Validates: Requirements 15.1**

Property 59: Время переключения между разделами
*Для любого* переключения между разделами, новый экран должен отображаться менее чем за 0.5 секунды
**Validates: Requirements 15.2**

### Логирование

Property 60: Логирование важных событий
*Для любого* важного события (запуск, ошибка, операция с БД), событие должно быть залогировано
**Validates: Requirements 16.1**

Property 61: Язык логов
*Для любого* лог-сообщения, текст должен быть на русском языке
**Validates: Requirements 16.4**

Property 62: Контекст в логах
*Для любого* лог-сообщения, должен быть включён контекст: модуль, функция, параметры операции
**Validates: Requirements 16.5**
## Error Handling

### Стратегия обработки ошибок

**Принципы:**
1. **Fail Fast на уровне системы** - немедленный отказ при обнаружении невалидных данных в бизнес-логике
2. **Сбор ошибок на уровне UX** - накопление всех ошибок валидации форм для отображения пользователю
3. **Структурированное логирование** - все ошибки логируются с полным контекстом на русском языке

### Типы ошибок

**1. Ошибки валидации (ValidationError)**
- Возникают при невалидных пользовательских вводах
- Обрабатываются на уровне Pydantic моделей
- Отображаются пользователю с понятным описанием проблемы
- Примеры: пустое название категории, отрицательная сумма, дата в будущем

**2. Ошибки бизнес-логики (BusinessLogicError)**
- Возникают при нарушении бизнес-правил
- Обрабатываются на уровне сервисов
- Отображаются пользователю с объяснением причины
- Примеры: удаление системной категории, удаление категории со связанными транзакциями

**3. Ошибки базы данных (DatabaseError)**
- Возникают при проблемах с БД (блокировки, нарушение ограничений)
- Логируются с полным контекстом
- Пользователю показывается общее сообщение без технических деталей
- Примеры: нарушение уникальности, ошибка подключения к БД

**4. Системные ошибки (SystemError)**
- Непредвиденные ошибки системы
- Логируются с полным stack trace
- Пользователю показывается общее сообщение с предложением повторить операцию
- Примеры: отсутствие файла конфигурации, ошибка инициализации

### Обработка ошибок по слоям

**UI Layer (Flet Components):**
```python
try:
    # Валидация на уровне UI
    if not self.validate_form():
        self.show_validation_errors()
        return
    
    # Вызов сервиса
    result = self.service.create_transaction(data)
    self.show_success_message()
    
except ValidationError as e:
    # Отображение ошибок валидации
    self.show_error_dialog("Ошибка валидации", str(e))
    
except BusinessLogicError as e:
    # Отображение бизнес-ошибок
    self.show_error_dialog("Операция невозможна", str(e))
    
except DatabaseError as e:
    # Логирование и общее сообщение
    logger.error(f"Ошибка БД при создании транзакции: {e}", extra=context)
    self.show_error_dialog("Ошибка", "Не удалось сохранить данные. Попробуйте ещё раз.")
    
except Exception as e:
    # Непредвиденные ошибки
    logger.exception(f"Непредвиденная ошибка: {e}", extra=context)
    self.show_error_dialog("Ошибка", "Произошла непредвиденная ошибка. Попробуйте перезапустить приложение.")
```

**Service Layer:**
```python
def delete_category(self, category_id: int) -> None:
    """
    Удаление категории с проверками бизнес-правил.
    
    Raises:
        BusinessLogicError: Если категория системная или имеет связанные транзакции
        DatabaseError: При ошибке БД
    """
    category = self.get_category(category_id)
    
    # Проверка бизнес-правил
    if category.is_system:
        raise BusinessLogicError("Нельзя удалить системную категорию")
    
    if self.has_transactions(category_id):
        raise BusinessLogicError("Нельзя удалить категорию с существующими транзакциями")
    
    try:
        self.repository.delete(category_id)
        logger.info(f"Категория удалена: {category.name}", extra={"category_id": category_id})
    except Exception as e:
        logger.error(f"Ошибка при удалении категории: {e}", extra={"category_id": category_id})
        raise DatabaseError(f"Не удалось удалить категорию") from e
```

### Логирование ошибок

**Формат лог-сообщений:**
```python
logger.error(
    "Ошибка при создании транзакции",
    extra={
        "module": "transaction_service",
        "function": "create_transaction",
        "user_id": user_id,
        "transaction_data": transaction_data,
        "error_type": type(e).__name__,
        "error_message": str(e)
    }
)
```

**Уровни логирования:**
- **ERROR**: Ошибки, требующие внимания (ошибки БД, бизнес-логики)
- **WARNING**: Потенциальные проблемы (просроченные платежи, кассовые разрывы)
- **INFO**: Важные события (создание транзакции, исполнение платежа)
- **DEBUG**: Детальная информация для отладки (только в dev-режиме)

## Testing Strategy

### Общий подход

Приложение использует **двойную стратегию тестирования**:
1. **Unit-тесты** - проверка конкретных примеров, граничных случаев и интеграции
2. **Property-based тесты** - проверка универсальных свойств на случайных данных

Оба типа тестов дополняют друг друга и обеспечивают комплексное покрытие.

### Property-Based Testing (PBT)

**Библиотека:** Hypothesis (Python)

**Конфигурация:**
- Минимум 100 итераций на каждый property-тест
- Каждый тест помечен комментарием с ссылкой на correctness property из design.md
- Формат комментария: `# Feature: flet-finance-tracker, Property {N}: {описание}`

**Примеры property-тестов:**

```python
from hypothesis import given, strategies as st
import hypothesis

# Feature: flet-finance-tracker, Property 6: Сохранение транзакции
@given(
    amount=st.floats(min_value=0.01, max_value=1000000),
    type=st.sampled_from([TransactionType.INCOME, TransactionType.EXPENSE]),
    category_id=st.integers(min_value=1, max_value=100),
    description=st.text(min_size=0, max_size=500),
    date=st.dates()
)
@hypothesis.settings(max_examples=100)
def test_transaction_persistence(amount, type, category_id, description, date):
    """
    Для любой валидной транзакции, после создания транзакция должна быть 
    сохранена в БД и доступна для чтения.
    """
    # Создание транзакции
    transaction_data = TransactionCreate(
        amount=amount,
        type=type,
        category_id=category_id,
        description=description,
        transaction_date=date
    )
    
    created = service.create_transaction(transaction_data)
    
    # Проверка персистентности
    retrieved = service.get_transaction(created.id)
    
    assert retrieved is not None
    assert retrieved.amount == amount
    assert retrieved.type == type
    assert retrieved.category_id == category_id
    assert retrieved.description == description
    assert retrieved.transaction_date == date


# Feature: flet-finance-tracker, Property 11: Уникальность названия категории
@given(
    name=st.text(min_size=1, max_size=100),
    type=st.sampled_from([TransactionType.INCOME, TransactionType.EXPENSE])
)
@hypothesis.settings(max_examples=100)
def test_category_name_uniqueness(name, type):
    """
    Для любой новой категории, если категория с таким названием уже существует,
    то создание должно быть отклонено с ошибкой валидации.
    """
    # Создание первой категории
    category1 = service.create_category(CategoryCreate(name=name, type=type))
    
    # Попытка создать дубликат
    with pytest.raises(ValidationError):
        service.create_category(CategoryCreate(name=name, type=type))
```

**Стратегии генерации данных:**
- Используются умные генераторы, ограничивающие входное пространство
- Генераторы учитывают бизнес-правила (например, категория отложенного платежа должна быть EXPENSE)
- Для дат используются реалистичные диапазоны (не слишком далеко в прошлом/будущем)

### Unit Testing

**Библиотека:** pytest

**Области покрытия:**
1. **UI компоненты** - тестирование отображения, взаимодействия, обработки событий
2. **Интеграция UI-сервисы** - проверка корректного вызова сервисов из UI
3. **Граничные случаи** - специфические сценарии, которые сложно покрыть property-тестами
4. **Ошибочные сценарии** - проверка обработки ошибок

**Примеры unit-тестов:**

```python
def test_calendar_displays_current_month():
    """Проверка отображения текущего месяца при открытии календаря."""
    calendar_view = CalendarView(db_session, transaction_service)
    calendar_view.build()
    
    assert calendar_view.current_month == datetime.now().month
    assert calendar_view.current_year == datetime.now().year


def test_system_category_deletion_blocked():
    """Проверка блокировки удаления системной категории."""
    system_category = create_system_category("Зарплата")
    
    with pytest.raises(BusinessLogicError, match="системную категорию"):
        service.delete_category(system_category.id)


def test_pending_payment_requires_expense_category():
    """Проверка валидации типа категории для отложенного платежа."""
    income_category = create_category("Доход", TransactionType.INCOME)
    
    with pytest.raises(ValidationError):
        service.create_pending_payment(
            PendingPaymentCreate(
                amount=1000,
                category_id=income_category.id,
                description="Тест"
            )
        )
```

### Переиспользование тестов из веб-версии

**Сервисы:**
- Все тесты сервисов из `finance_tracker_python/tests/` переиспользуются без изменений
- Сервисы идентичны веб-версии, поэтому тесты полностью применимы

**Модели:**
- Тесты моделей данных переиспользуются
- Тесты валидации Pydantic моделей переиспользуются

**Новые тесты:**
- Тесты UI компонентов (специфичны для Flet)
- Тесты интеграции UI-сервисы
- Тесты специфичной для десктопа функциональности (сохранение размера окна, конфигурация)

### Тестовые фикстуры

```python
@pytest.fixture
def db_session():
    """Создание тестовой БД в памяти."""
    engine = create_engine("sqlite:///:memory:")
    Base.metadata.create_all(engine)
    Session = sessionmaker(bind=engine)
    session = Session()
    yield session
    session.close()


@pytest.fixture
def sample_categories(db_session):
    """Создание набора тестовых категорий."""
    categories = [
        CategoryDB(name="Зарплата", type=TransactionType.INCOME, is_system=True),
        CategoryDB(name="Продукты", type=TransactionType.EXPENSE, is_system=False),
        CategoryDB(name="Транспорт", type=TransactionType.EXPENSE, is_system=False),
    ]
    for cat in categories:
        db_session.add(cat)
    db_session.commit()
    return categories


@pytest.fixture
def transaction_service(db_session):
    """Создание сервиса транзакций для тестов."""
    return TransactionService(db_session)
```

### Запуск тестов

```bash
# Все тесты
pytest

# Только property-тесты
pytest -m property

# Только unit-тесты
pytest -m unit

# С покрытием
pytest --cov=finance_tracker_flet --cov-report=html

# Конкретный модуль
pytest tests/test_calendar_view.py
```

## Implementation Notes

### Инкрементальная разработка

**Порядок реализации подсистем:**

**Фаза 1: Базовая функциональность (MVP)**
1. Инициализация проекта, настройка БД
2. Модели данных (Transaction, Category)
3. Сервисы (TransactionService, CategoryService)
4. Главное окно с навигацией (MainWindow)
5. Компоненты календаря:
   - CalendarWidget (календарь с индикаторами)
   - TransactionsPanel (панель транзакций дня)
   - CalendarLegend (легенда)
   - TransactionModal (модальное окно транзакции)
6. Главный экран (HomeView) - интеграция компонентов
7. Управление категориями (CategoriesView)

**Фаза 2: Плановые транзакции**
8. Модели (PlannedTransaction, RecurrenceRule, PlannedOccurrence)
9. Сервисы (PlannedTransactionService, RecurrenceService)
10. Компоненты:
    - ExecuteOccurrenceModal (исполнение вхождения)
    - PlannedTransactionModal (создание/редактирование)
    - PlannedTransactionsWidget (виджет для главного экрана)
11. UI (PlannedTransactionsView)
12. Интеграция с главным экраном (HomeView)

**Фаза 3: Прогноз и анализ**
13. Сервис прогноза баланса (BalanceForecastService)
14. Интеграция прогноза с календарём и TransactionsPanel
15. План-факт анализ (PlanFactView, PlanFactService)

**Фаза 4: Отложенные платежи**
16. Модель (PendingPayment)
17. Сервис (PendingPaymentService)
18. Компоненты:
    - PendingPaymentModal (создание/редактирование)
    - ExecutePendingPaymentModal (исполнение)
    - PendingPaymentsWidget (виджет для главного экрана)
19. UI (PendingPaymentsView)
20. Интеграция с главным экраном (HomeView)

**Фаза 5: Кредиты**
21. Модели (Lender, Loan, LoanPayment)
22. Сервисы (LenderService, LoanService, LoanPaymentService, LoanStatisticsService)
23. UI (LoansView, LoanDetailsView, LendersView)
24. Интеграция с главным экраном (отображение платежей в TransactionsPanel)

**Фаза 6: Полировка**
25. Настройки приложения (SettingsView)
26. Логирование (структурированное, на русском)
27. Оптимизация производительности (кэширование, индексы)
28. Тестирование (unit + property-based)
29. Упаковка для Windows и Android

### Технические решения

**Управление состоянием:**
- Использование Flet state management для реактивности UI
- Локальное состояние в компонентах для UI-специфичных данных
- Глобальное состояние для текущего баланса, выбранной даты

**Кэширование:**
- Категории кэшируются при запуске приложения (редко меняются)
- Займодатели кэшируются при открытии раздела кредитов
- Транзакции текущего месяца кэшируются для быстрого отображения календаря

**Оптимизация запросов к БД:**
- Использование индексов на часто запрашиваемых полях (transaction_date, category_id)
- Eager loading для связанных данных (транзакции с категориями)
- Пагинация для больших списков (плановые транзакции, история платежей)

**Конфигурация:**
```python
# config.py
import os
from pathlib import Path

class Config:
    # Путь к БД
    DB_PATH = os.getenv("FINANCE_TRACKER_DB", 
                        str(Path.home() / ".finance_tracker" / "finance.db"))
    
    # Логирование
    LOG_LEVEL = os.getenv("LOG_LEVEL", "INFO")
    LOG_FILE = str(Path.home() / ".finance_tracker" / "app.log")
    
    # UI
    WINDOW_WIDTH = 1200
    WINDOW_HEIGHT = 800
    THEME_MODE = "light"  # light/dark
    
    # Производительность
    CACHE_CATEGORIES = True
    TRANSACTIONS_PER_PAGE = 50
```

### Адаптация для Android

**Отличия Android-версии:**
1. **Адаптивный UI** - использование ResponsiveRow для адаптации под размер экрана
2. **Навигация** - BottomNavigationBar вместо NavigationRail
3. **Жесты** - поддержка свайпов для навигации между месяцами
4. **Хранилище** - БД в app-specific storage Android
5. **Уведомления** - напоминания о просроченных платежах

**Проверка платформы:**
```python
import flet as ft

def is_mobile():
    return ft.platform in [ft.Platform.ANDROID, ft.Platform.IOS]

def build_navigation():
    if is_mobile():
        return ft.BottomNavigationBar(...)
    else:
        return ft.NavigationRail(...)
```

## Deployment

### Windows (.exe)

**Инструмент:** PyInstaller или Flet build

```bash
# Установка зависимостей
pip install -r requirements.txt

# Сборка
flet build windows

# Или с PyInstaller
pyinstaller --onefile --windowed --name="Finance Tracker" main.py
```

**Структура дистрибутива:**
```
FinanceTracker/
├── FinanceTracker.exe
├── README.txt
└── LICENSE.txt
```

### Android (.apk)

**Инструмент:** Flet build

```bash
# Сборка APK
flet build apk

# Сборка AAB (для Google Play)
flet build aab
```

**Требования:**
- Android SDK
- Java JDK 11+
- Gradle

### Конфигурация для сборки

```yaml
# pubspec.yaml (для Flet)
name: finance_tracker
description: Приложение для управления личными финансами

dependencies:
  flutter:
    sdk: flutter
  flet: ^0.10.0

flutter:
  uses-material-design: true
```

## Future Enhancements

**Возможные улучшения (не входят в текущий scope):**

1. **Синхронизация между устройствами** - облачное хранилище для синхронизации данных
2. **Импорт из банковских выписок** - автоматический импорт транзакций из CSV/OFX
3. **Бюджетирование** - установка лимитов по категориям
4. **Графики и визуализация** - диаграммы расходов по категориям, динамика баланса
5. **Экспорт отчётов** - экспорт в Excel/PDF
6. **Мультивалютность** - поддержка нескольких валют с конвертацией
7. **Резервное копирование** - автоматическое резервное копирование БД
8. **Темы оформления** - дополнительные цветовые схемы
9. **Голосовой ввод** - добавление транзакций голосом (для мобильной версии)
10. **Уведомления** - push-уведомления о просроченных платежах, кассовых разрывах
