# Requirements Document

## Introduction

Портирование веб-приложения Finance Tracker (Flask + FastAPI + Alpine.js) на десктопное приложение с использованием Python + Flet. Цель — создать кроссплатформенное десктопное приложение с сохранением всей функциональности оригинального веб-приложения, но с нативным UI и улучшенным UX для десктопа.

**Подход к портированию:** Инкрементальная разработка по подсистемам. Каждая подсистема портируется полностью (модели + сервисы + UI) и должна быть запускаемой и тестируемой независимо. Порядок портирования:
1. Календарь и транзакции (базовая функциональность)
2. Категории
3. Плановые транзакции
4. Отложенные платежи
5. Займодатели и кредиты
6. План-факт анализ
7. Статистика и отчёты

## Glossary

- **Finance Tracker**: Приложение для управления личными финансами
- **Flet**: Python фреймворк для создания кроссплатформенных десктопных и веб-приложений на основе Flutter
- **Transaction**: Финансовая транзакция (доход или расход)
- **Planned Transaction**: Плановая транзакция (однократная или периодическая)
- **Occurrence**: Вхождение плановой транзакции (конкретная дата исполнения)
- **Pending Payment**: Отложенный платёж без конкретной даты или с плановой датой
- **Loan**: Кредит или займ
- **Lender**: Займодатель (банк, МФО, физическое лицо)
- **Category**: Категория для классификации транзакций
- **SQLite**: Локальная база данных для хранения всех данных
- **Recurrence Rule**: Правило повторения для периодических транзакций

## Requirements

### Requirement 1: Архитектура и инфраструктура

**User Story:** Как разработчик, я хочу иметь чистую архитектуру приложения с инкрементальной разработкой по подсистемам, чтобы код был поддерживаемым и каждая подсистема была запускаемой независимо.

#### Acceptance Criteria

1. THE System SHALL копировать необходимые модели данных из finance_tracker_python/src/models.py для каждой подсистемы
2. THE System SHALL копировать необходимые сервисы из finance_tracker_python/src/services/ для каждой подсистемы
3. THE System SHALL использовать SQLite базу данных с той же схемой, что и веб-версия
4. THE System SHALL организовать код в модули: UI компоненты (views/), сервисы (services/), модели (models/), утилиты (utils/)
5. THE System SHALL обеспечить разделение бизнес-логики (сервисы) и UI логики (Flet компоненты)
6. WHEN разработчик завершает подсистему THEN THE System SHALL быть запускаемым и демонстрировать работу этой подсистемы
7. THE System SHALL иметь модульную структуру, позволяющую добавлять новые подсистемы без изменения существующих

### Requirement 2: Главное окно и навигация

**User Story:** Как пользователь, я хочу иметь удобную навигацию между разделами приложения, чтобы быстро переключаться между функциями.

#### Acceptance Criteria

1. WHEN приложение запускается THEN THE System SHALL отображать главное окно с боковой панелью навигации
2. THE System SHALL предоставлять навигацию только по реализованным разделам (изначально только Календарь)
3. WHEN пользователь выбирает раздел в навигации THEN THE System SHALL отображать соответствующий экран без перезагрузки приложения
4. THE System SHALL сохранять последний выбранный раздел между сеансами работы
5. THE System SHALL отображать текущий баланс в заголовке приложения
6. THE System SHALL добавлять новые разделы в навигацию по мере реализации подсистем

### Requirement 3: Календарь и транзакции

**User Story:** Как пользователь, я хочу видеть календарь с финансовой активностью и управлять транзакциями, чтобы отслеживать свои доходы и расходы.

#### Acceptance Criteria

1. WHEN пользователь открывает раздел Календарь THEN THE System SHALL отображать календарь текущего месяца с визуальными индикаторами
2. THE System SHALL отображать индикаторы на днях календаря: зелёный для доходов, красный для расходов, жёлтый/оранжевый для кассовых разрывов
3. WHEN пользователь кликает на день THEN THE System SHALL отображать список транзакций этого дня
4. WHEN пользователь создаёт новую транзакцию THEN THE System SHALL валидировать данные и сохранять транзакцию в БД
5. WHEN пользователь редактирует транзакцию THEN THE System SHALL обновлять данные в БД и обновлять UI
6. WHEN пользователь удаляет транзакцию THEN THE System SHALL удалять запись из БД и обновлять UI
7. THE System SHALL поддерживать навигацию между месяцами (вперёд/назад)
8. THE System SHALL выделять текущий день в календаре

### Requirement 4: Управление категориями

**User Story:** Как пользователь, я хочу управлять справочником категорий, чтобы классифицировать свои транзакции.

#### Acceptance Criteria

1. WHEN пользователь открывает раздел Категории THEN THE System SHALL отображать список всех категорий
2. THE System SHALL поддерживать фильтрацию категорий по типу (все, доходы, расходы)
3. WHEN пользователь создаёт новую категорию THEN THE System SHALL валидировать уникальность названия и сохранять в БД
4. WHEN пользователь пытается удалить системную категорию THEN THE System SHALL предотвращать удаление и показывать сообщение об ошибке
5. WHEN пользователь удаляет пользовательскую категорию THEN THE System SHALL проверять наличие связанных транзакций и предотвращать удаление при их наличии
6. THE System SHALL отображать тип категории (доход/расход) с цветовой индикацией

### Requirement 5: Плановые транзакции

**User Story:** Как пользователь, я хочу создавать плановые транзакции с правилами повторения, чтобы автоматизировать учёт регулярных доходов и расходов.

#### Acceptance Criteria

1. WHEN пользователь создаёт плановую транзакцию THEN THE System SHALL поддерживать типы повторения: однократная, ежедневная, еженедельная, ежемесячная, ежегодная, кастомная
2. THE System SHALL поддерживать условия окончания: бессрочно, до даты, после N повторений
3. WHEN пользователь создаёт периодическую транзакцию THEN THE System SHALL автоматически генерировать вхождения на основе правила повторения
4. WHEN пользователь исполняет вхождение THEN THE System SHALL создавать фактическую транзакцию и обновлять статус вхождения
5. WHEN пользователь пропускает вхождение THEN THE System SHALL обновлять статус на SKIPPED и сохранять причину пропуска
6. THE System SHALL отображать список плановых транзакций с возможностью редактирования и удаления
7. THE System SHALL отображать вхождения в календаре с визуальными индикаторами

### Requirement 6: Прогноз баланса

**User Story:** Как пользователь, я хочу видеть прогноз баланса с учётом плановых транзакций, чтобы предотвращать кассовые разрывы.

#### Acceptance Criteria

1. WHEN пользователь просматривает календарь THEN THE System SHALL рассчитывать прогнозируемый баланс для каждого дня
2. THE System SHALL учитывать фактические транзакции, плановые вхождения и отложенные платежи с датой
3. WHEN прогнозируемый баланс отрицательный THEN THE System SHALL выделять день в календаре жёлтым/оранжевым цветом
4. THE System SHALL отображать текущий баланс и прогноз на выбранную дату

### Requirement 7: План-факт анализ

**User Story:** Как пользователь, я хочу сравнивать плановые и фактические транзакции, чтобы анализировать исполнение бюджета.

#### Acceptance Criteria

1. WHEN пользователь открывает раздел План-факт анализ THEN THE System SHALL отображать список вхождений с их статусами
2. THE System SHALL поддерживать фильтрацию по периоду (дата начала, дата окончания)
3. THE System SHALL поддерживать фильтрацию по категории
4. THE System SHALL рассчитывать отклонения по сумме и дате для исполненных вхождений
5. THE System SHALL отображать статистику: процент вовремя, пропущенные, средние отклонения
6. WHEN пользователь кликает на вхождение THEN THE System SHALL отображать детальную информацию

### Requirement 8: Отложенные платежи

**User Story:** Как пользователь, я хочу управлять отложенными платежами, чтобы отслеживать расходы без конкретной даты.

#### Acceptance Criteria

1. WHEN пользователь создаёт отложенный платёж THEN THE System SHALL валидировать, что категория имеет тип EXPENSE
2. THE System SHALL поддерживать приоритеты: Low, Medium, High, Critical
3. THE System SHALL поддерживать опциональную плановую дату
4. WHEN пользователь исполняет платёж THEN THE System SHALL создавать фактическую транзакцию и обновлять статус
5. WHEN пользователь отменяет платёж THEN THE System SHALL обновлять статус на CANCELLED и сохранять причину
6. THE System SHALL отображать список активных платежей с сортировкой по приоритету и дате
7. THE System SHALL отображать статистику: общая сумма активных платежей, распределение по приоритетам

### Requirement 9: Управление займодателями

**User Story:** Как пользователь, я хочу управлять справочником займодателей, чтобы отслеживать источники кредитов.

#### Acceptance Criteria

1. WHEN пользователь создаёт займодателя THEN THE System SHALL валидировать уникальность названия
2. THE System SHALL поддерживать типы: Банк, МФО, Физическое лицо, Другое
3. THE System SHALL сохранять контактную информацию и примечания
4. WHEN пользователь удаляет займодателя THEN THE System SHALL проверять наличие связанных кредитов и предотвращать удаление при их наличии
5. THE System SHALL отображать список займодателей с возможностью редактирования

### Requirement 10: Управление кредитами

**User Story:** Как пользователь, я хочу управлять кредитами и платежами по ним, чтобы отслеживать долговую нагрузку.

#### Acceptance Criteria

1. WHEN пользователь создаёт кредит THEN THE System SHALL валидировать обязательные поля: займодатель, сумма, дата выдачи
2. THE System SHALL поддерживать типы кредитов: Микрокредит, Потребительский, Ипотека, Личный займ, Другое
3. THE System SHALL автоматически рассчитывать дату окончания на основе срока в месяцах
4. THE System SHALL поддерживать статусы: Активный, Погашен, Просрочен
5. WHEN пользователь создаёт кредит THEN THE System SHALL автоматически генерировать график платежей
6. THE System SHALL отображать детальную информацию о кредите: график платежей, историю, статистику
7. THE System SHALL поддерживать досрочное погашение (полное и частичное)

### Requirement 11: Платежи по кредитам

**User Story:** Как пользователь, я хочу управлять платежами по кредитам, чтобы контролировать выплаты.

#### Acceptance Criteria

1. WHEN пользователь исполняет платёж THEN THE System SHALL создавать фактическую транзакцию расхода
2. THE System SHALL поддерживать статусы платежей: Ожидается, Выполнен, Выполнен с просрочкой, Просрочен, Отменён
3. WHEN текущая дата больше плановой даты платежа THEN THE System SHALL автоматически обновлять статус на Просрочен
4. THE System SHALL рассчитывать количество дней просрочки
5. THE System SHALL поддерживать импорт платежей из CSV файла
6. THE System SHALL отображать платежи в календаре с индикатором 💳
7. THE System SHALL выделять просроченные платежи в календаре

### Requirement 12: Статистика по кредитам

**User Story:** Как пользователь, я хочу видеть статистику по кредитам, чтобы оценивать свою долговую нагрузку.

#### Acceptance Criteria

1. THE System SHALL рассчитывать общую сумму активных кредитов
2. THE System SHALL рассчитывать среднемесячные платежи
3. THE System SHALL рассчитывать кредитную нагрузку (процент от дохода)
4. THE System SHALL рассчитывать просроченную задолженность
5. THE System SHALL отображать статистику за выбранный период
6. THE System SHALL отображать распределение кредитов по типам

### Requirement 13: Настройки и конфигурация

**User Story:** Как пользователь, я хочу настраивать приложение под свои предпочтения, чтобы работать комфортно.

#### Acceptance Criteria

1. THE System SHALL сохранять путь к базе данных в конфигурационном файле
2. THE System SHALL поддерживать выбор темы оформления (светлая/тёмная)
3. THE System SHALL сохранять размер и положение окна между сеансами
4. THE System SHALL сохранять последний выбранный раздел навигации
5. THE System SHALL поддерживать настройку формата отображения дат и сумм

### Requirement 14: Обработка ошибок и валидация

**User Story:** Как пользователь, я хочу получать понятные сообщения об ошибках, чтобы исправлять свои действия.

#### Acceptance Criteria

1. WHEN происходит ошибка валидации THEN THE System SHALL отображать сообщение с описанием проблемы
2. WHEN происходит ошибка базы данных THEN THE System SHALL логировать детали и показывать общее сообщение пользователю
3. THE System SHALL валидировать все пользовательские вводы перед сохранением в БД
4. THE System SHALL предотвращать некорректные операции (удаление системных категорий, удаление категорий с транзакциями)
5. THE System SHALL использовать Pydantic модели для валидации данных

### Requirement 15: Производительность и оптимизация

**User Story:** Как пользователь, я хочу, чтобы приложение работало быстро, чтобы не тратить время на ожидание.

#### Acceptance Criteria

1. WHEN приложение запускается THEN THE System SHALL инициализироваться менее чем за 2 секунды
2. WHEN пользователь переключается между разделами THEN THE System SHALL отображать новый экран менее чем за 0.5 секунды
3. THE System SHALL использовать индексы БД для быстрого поиска транзакций
4. THE System SHALL кэшировать часто используемые данные (категории, займодатели)
5. THE System SHALL обновлять только изменённые части UI при изменении данных

### Requirement 16: Логирование и отладка

**User Story:** Как разработчик, я хочу иметь подробное логирование, чтобы отлаживать проблемы.

#### Acceptance Criteria

1. THE System SHALL логировать все важные события: запуск, ошибки, операции с БД
2. THE System SHALL использовать структурированное логирование с уровнями: DEBUG, INFO, WARNING, ERROR
3. THE System SHALL сохранять логи в файл с ротацией
4. THE System SHALL логировать на русском языке
5. THE System SHALL включать контекст в логи: модуль, функция, параметры операции

### Requirement 17: Тестирование

**User Story:** Как разработчик, я хочу иметь автоматические тесты, чтобы гарантировать корректность работы.

#### Acceptance Criteria

1. THE System SHALL иметь юнит-тесты для всех UI компонентов
2. THE System SHALL иметь интеграционные тесты для взаимодействия UI и сервисов
3. THE System SHALL переиспользовать существующие тесты сервисов из веб-версии
4. THE System SHALL иметь тесты для валидации данных
5. THE System SHALL иметь тесты для обработки ошибок

### Requirement 18: Упаковка и распространение

**User Story:** Как пользователь, я хочу иметь исполняемый файл приложения для Windows и Android, чтобы не устанавливать Python и зависимости.

#### Acceptance Criteria

1. THE System SHALL поддерживать упаковку в исполняемый файл для Windows (.exe)
2. THE System SHALL поддерживать упаковку для Android (.apk)
3. WHEN приложение запускается из исполняемого файла THEN THE System SHALL создавать БД в директории пользователя
4. THE System SHALL включать все необходимые зависимости в исполняемый файл
5. THE System SHALL адаптировать UI для мобильных устройств при запуске на Android
